<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.kuonjiarisu.backend.mapper.UserMapper">
  <resultMap id="userAccountResultMap" type="io.github.kuonjiarisu.backend.auth.model.UserAccount">
    <constructor>
      <arg column="id" javaType="java.lang.String" />
      <arg column="username" javaType="java.lang.String" />
      <arg column="password_hash" javaType="java.lang.String" />
      <arg column="role" javaType="java.lang.String" />
      <arg column="display_name" javaType="java.lang.String" />
      <arg column="enabled" javaType="java.lang.Boolean" />
      <arg column="last_login_at" javaType="java.time.LocalDateTime" />
      <arg column="created_at" javaType="java.time.LocalDateTime" />
      <arg column="updated_at" javaType="java.time.LocalDateTime" />
    </constructor>
  </resultMap>

  <select id="findByUsername" resultMap="userAccountResultMap">
    SELECT id, username, password_hash, role, display_name, enabled, last_login_at, created_at, updated_at
    FROM users
    WHERE username = #{username}
  </select>

  <select id="findById" resultMap="userAccountResultMap">
    SELECT id, username, password_hash, role, display_name, enabled, last_login_at, created_at, updated_at
    FROM users
    WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="io.github.kuonjiarisu.backend.auth.model.UserAccount">
    INSERT INTO users (
      id, username, password_hash, role, display_name, enabled, last_login_at, created_at, updated_at
    ) VALUES (
      #{id}, #{username}, #{passwordHash}, #{role}, #{displayName}, #{enabled}, #{lastLoginAt}, #{createdAt}, #{updatedAt}
    )
  </insert>

  <update id="updateLastLoginAt">
    UPDATE users
    SET last_login_at = #{lastLoginAt},
        updated_at = #{lastLoginAt}
    WHERE id = #{id}
  </update>
</mapper>
